import 'package:flutter/material.dart';

class ChatStatusUpdatePage extends StatelessWidget {
  const ChatStatusUpdatePage({super.key});

  @override
  Widget build(BuildContext context) => Scaffold(
    appBar: AppBar(title: const Text('Update status')),
    body: const Center(child: Text('Not implemented yet.')),
  );
}
