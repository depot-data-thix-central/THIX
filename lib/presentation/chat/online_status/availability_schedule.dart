import 'package:flutter/material.dart';

class AvailabilitySchedule extends StatelessWidget {
  const AvailabilitySchedule({super.key});

  @override
  Widget build(BuildContext context) => const Scaffold(
    appBar: AppBar(title: Text('Availability schedule')),
    body: Center(child: Text('Not implemented yet.')),
  );
}
