import 'package:flutter/material.dart';

class ChatWidgetConfig extends StatelessWidget {
  const ChatWidgetConfig({super.key});

  @override
  Widget build(BuildContext context) => const Scaffold(
    appBar: AppBar(title: Text('Chat widget config')),
    body: Center(child: Text('Not implemented yet.')),
  );
}
