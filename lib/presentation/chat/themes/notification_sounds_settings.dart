import 'package:flutter/material.dart';

class NotificationSoundsSettings extends StatelessWidget {
  const NotificationSoundsSettings({super.key});

  @override
  Widget build(BuildContext context) => const Scaffold(
    appBar: AppBar(title: Text('Notification sounds')),
    body: Center(child: Text('Not implemented yet.')),
  );
}
